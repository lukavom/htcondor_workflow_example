[project]
name = "example-workflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["snakemake>=9"]

[tool.pixi.dependencies]
conda = "*"


[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

